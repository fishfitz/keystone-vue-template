/*
    This does export an empty object.
    Some plugins are aliased to this file by webpack for server bundle,
    when they are not compatible with SSR, so they are ignored.
*/

export default {};
